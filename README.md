# simple-mailer
A PHP Mailer wrapper that adds validations to simplify sending emails in bulk.

## Preparing the development environment

### Installing PHP dependencies in ubuntu

```shell
sudo apt-get update && sudo apt-get install -y php composer php-curl php-mysql php-mbstring php-zip php-dom php-gd
```

### Install composer dependencies

```shell
composer install
```
